import React from "react";
import Navbar from "./components/navbar";

interface Props {}

function App(props: Props) {
  const {} = props;

  return (
    <main>
      <Navbar/>
    </main>
  );
}

export default App;
